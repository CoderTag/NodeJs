let anotherCounter = (arr)=>{
    console.log(`The array is having ${arr.length} elements`);
}

let anotherAdd = (a,b)=>{
    console.log(`Addition of two numbers is ${a + b}`);
}

anotherPI = 3.18

module.exports = {
    anotherCounter: anotherCounter,
    anotherAdd: anotherAdd,
    anotherPI: anotherPI
}